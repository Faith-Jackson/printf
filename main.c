#include "main.h"

int main(void)
{
    int len;

    len = _printf("Let's test our _printf function: %s, %c, %%\n", "Hello", 'A');
    _printf("Number of characters printed: %d\n", len);

    return count;
}

